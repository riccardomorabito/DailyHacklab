/**
 * Configuration constants for the application
 */

/**
 * localStorage key for storing the admin setting that allows/disallows the public registration UI.
 * Value is 'true' or 'false' as a string.
 * 
 * @example
 * // Enable public registration
 * localStorage.setItem(PUBLIC_REGISTRATION_STORAGE_KEY, 'true');
 * 
 * // Disable public registration
 * localStorage.setItem(PUBLIC_REGISTRATION_STORAGE_KEY, 'false');
 */
export const PUBLIC_REGISTRATION_STORAGE_KEY = 'hacklab-daily-allow-public-registration';
